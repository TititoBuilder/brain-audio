[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "brain-audio"
version = "0.1.0"
description = "Shared TTS normalization utilities for BRAIN_OS projects"
requires-python = ">=3.10"
